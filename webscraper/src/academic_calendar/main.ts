const main = (coursePermission: Boolean, programPermission: Boolean) => {
    console.log(`Course Scraping: ${coursePermission}`)
    console.log(`Program Scraping: ${programPermission}`)
}

export default main;